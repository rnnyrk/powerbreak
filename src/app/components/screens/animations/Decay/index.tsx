import React, { useEffect, useState } from 'react';
import Animated, { Easing } from 'react-native-reanimated';

import { DecayContainer } from './styled';

const Decay: React.FC<DecayProps> = () => {
  return (
    <DecayContainer>
    </DecayContainer>
  );
};

type DecayProps = {};

export default Decay;
