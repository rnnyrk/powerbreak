import Animated from 'react-native-reanimated';
import styled from 'styled-components';

import { Container } from 'common/general';

export const DecayContainer = styled(Container)`
  padding: 0;
`;
